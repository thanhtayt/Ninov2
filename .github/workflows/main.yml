name: Push file

on:
  push:
    branches: [ main ]

jobs:
  push-file:
    runs-on: ubuntu-latest
    steps:

      • uses: actions/checkout@v3

      • name: Push file
        run: |
          echo "Hello, world!" > hello.txt
          git add hello.txt
          git commit -m "Add hello.txt"
          git push
